#!/bin/bash

PEER_KEY=$1

if [ ! -e "/etc/fastd/{{ item.key }}/fastd-blacklist.json" ]; then
		exit 0
fi

if /bin/grep -Fq $PEER_KEY /etc/fastd/{{ item.key }}/fastd-blacklist.json; then
        exit 1
else
        exit 0
fi
